% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biovolume_cache.R
\name{recompute_biovolume_data}
\alias{recompute_biovolume_data}
\title{Recompute biovolume data, using the session cache when available}
\usage{
recompute_biovolume_data(
  rv,
  config,
  taxa_lookup,
  non_bio_classes = character(0)
)
}
\arguments{
\item{rv}{Reactive values (or list-like) with \code{classifications},
\code{custom_classes}, \code{biovolume_cache}, \code{diatom_status}.}

\item{config}{Reactive values (or list-like) with
\code{local_storage_path} and \code{pixels_per_micron}.}

\item{taxa_lookup}{Taxa lookup data.frame (already merged with custom
taxa where applicable).}

\item{non_bio_classes}{Character vector of non-biological classes.}
}
\value{
A data.frame as returned by \code{summarize_biovolumes()}.
}
\description{
Central entry point for summary recomputation (report generation, sample
exclusions, stale-summary refresh). Uses the in-memory per-ROI cache built
at load time; falls back to the file-based \code{summarize_biovolumes()}
when no cache exists. Updates \code{rv$diatom_status} with any newly
resolved classes as a side effect.
}

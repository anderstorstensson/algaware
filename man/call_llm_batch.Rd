% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_api.R
\name{call_llm_batch}
\alias{call_llm_batch}
\title{Call an LLM provider for a batch of independent prompts}
\usage{
call_llm_batch(prompts, provider = NULL, temperature = 0.3, on_progress = NULL)
}
\arguments{
\item{prompts}{List of prompts, each a list with \code{system} and
\code{user} strings.}

\item{provider}{Character string: \code{"openai"} or \code{"gemini"}.
NULL (default) auto-detects.}

\item{temperature}{Sampling temperature (default: 0.3).}

\item{on_progress}{Optional callback \code{function(i, n)} invoked
before each request in the sequential fallback (not used in the
parallel path, where requests run at once).}
}
\value{
A list the same length as \code{prompts}; each element is a
list with \code{text} (character or NULL) and \code{error}
(character or NULL).
}
\description{
For providers that support it (OpenAI), all requests are performed
concurrently with \code{httr2::req_perform_parallel()}, collapsing n
sequential round trips into roughly the latency of the slowest one.
Other providers fall back to a sequential loop (Gemini keeps its
rate-limit delay). Each prompt gets an independent result: one failed
request never aborts the batch.
}
\keyword{internal}
